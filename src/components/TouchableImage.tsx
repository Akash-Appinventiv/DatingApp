import {StyleSheet, Text, TouchableOpacity, View} from 'react-native';
import React from 'react';

const TouchableImage = () => {
  return (
    <TouchableOpacity>
      <Text>TouchableImage</Text>
    </TouchableOpacity>
  );
};

export default TouchableImage;

const styles = StyleSheet.create({});
